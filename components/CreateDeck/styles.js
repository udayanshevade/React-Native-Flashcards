import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  createContainer: {
    flexDirection: 'row',
    margin: 10,
  },
  button: {
    flex: 1,
  },
});

export default styles;
